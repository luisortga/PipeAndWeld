import time

for i in range(0, 16): print(f'INOX. {i}')
time.sleep(12)

iteration: tuple[int, int] = (0, 24)

for i in iteration: print(f'salida desde una tuple')

new_tuple: tuple[int, int] = (12, 16)

# continue

# set
conjunto_time = {12, 16, 4, 48}